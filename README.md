# 신사 이벤트 참여봇 V2 Railway용

업로드 후 Railway가 `python main.py`로 실행됩니다.

필수 환경변수:
- BOT_TOKEN
- ADMIN_ID

DB 보존용 권장 설정:
- DB_FILE=/data/event_bot.db

배포 후 텔레그램에서 `/admin`을 입력하세요.
`/start` 메시지 아래에 '내 신청 상태 / 참여 방법' 버튼이 보여야 새 버전이 실행 중인 것입니다.
